lxml>=4.2.4
numpy>=1.20.0
mido>=1.2.10
soundfile>=0.12.0
sounddevice>=0.4.6

# Optional: Heavy dependencies for full pipeline
# Install these when you need stem separation and audio-to-MIDI:
#   pip install demucs basic-pitch
#
# demucs>=4.0.0        # Meta's stem separation (requires PyTorch)
# basic-pitch>=0.3.0   # Spotify's audio-to-MIDI transcription
